#include <stdio.h>
int main(void) {
	int len;
	scanf("%d", &len);
	int *arr=(int*)malloc(len*sizeof(int));
	int i, am=1;
	for (i=0; i<len; i++)
	{
	    scanf("%d", &arr[i]);
	}
	int ntemp=0, amtemp=0;
	for (i=0; i<len; i++)
	{
		if (arr[i] == arr[i+1]) 
		{
			am++;
		}
		if ( amtemp < am )
		{
			amtemp=am;
			ntemp=arr[i];
		}
	}
	printf("%d",amtemp);
	return 0;
}
