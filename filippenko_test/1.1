1.1.
#include <stdio.h>

int main()
{
    int a,b,c;
    int k;
  printf("Введите числа a и b", a, b);
    scanf("%d%d",&a,&b);
    
    for(k=1;k<=b;k++)
    {
        c=a*a;
    }
    
    printf("c = ", c);
}
