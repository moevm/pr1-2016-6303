#include <stdio.h>

int main()
{
    int n=0,i=0,j=0;
    scanf("%d", &n);
    int **ma=(int**)malloc(sizeof(int*)*n);
    for (i=0; i<n; i++)
    {
        int *str=(int*)malloc(sizeof(int)*n);
        for (j=0; j<n; j++)
        {
            str[j]=i+j+1;
            printf("%d",str[j]);
        }
        printf("\n");
    }
    return 0;
}
