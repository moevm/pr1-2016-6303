#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{  char arr[100];  
int i, count = 0; 
str = gets(arr); 
char d; 
scanf("%c", &d); 
for (i=0; i<strlen(arr); i++)  
if (arr[i] == d)  
count ++; 
printf ("%d\n", count);
}
