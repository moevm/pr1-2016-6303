#include <stdio.h>
#include <string.h>
int main()
{    int len;
scanf ("%d", &len);
char arr[len];
int i=0; 
int result;  
for (i=0; i<len; ++i)  
{      
scanf ("%d", &arr[i]);  
}   
int max=0;  
int min=0; 
//  scanf("%s", &arr); 
for (i=0; i<len; ++i) 
{       
if (arr[i]>max)    
max=arr[i];  
}  
//  printf ("%d", max);  
min = max;   
for (i=0; i<len; ++i) 
{            
if (arr[i]<min)     
min=arr[i];  
}
//printf ("%d", min);
result = max-min;
printf ("%d" , result);   
return 0;}
