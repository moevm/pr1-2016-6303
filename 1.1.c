#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    long long int c;
    c = pow(a,b);
    printf("%d", c);
    return 0;
}
