#include <stdio.h>
int main()
{
    int n=0,i=0,j=0;
    scanf("%d", &n);
    int **ma=(int**)malloc(sizeof(int*)*n);
    for (i=0; i<n; i++)
    {
        int *str=(int*)malloc(sizeof(int)*n);
        for (j=0; j<n; j++)
        {
            if ( (i==j)&&(i==0) ) {printf("1");}
            else 
            {
                if ((j==n-1)&&(i==n-1)) {printf("1");}
                else 
                {   
                    if ( (i==0)&&(j==n-1) || (i==n-1)&&(j==0) ) {printf("2");}
                    else printf("0");
                }
            }
        }
        printf("\n");
    }
    return 0;
}
