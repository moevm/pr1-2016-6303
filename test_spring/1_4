#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
  char str[100];
  int i;
  str = gets(str);
  char sym;
  scanf("%c", &s);
  for (i=0; i<strlen(str); i++)
    if (str[i] != s)
    {
      printf("%c\n",str[i]);
    }
    else
    {
      continue;
    }
}
